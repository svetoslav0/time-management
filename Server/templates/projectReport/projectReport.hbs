<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Report</title>
       <style>
        body { font-family: Arial, sans-serif; }
        table { width: 100%; border-collapse: collapse; }
        th, td { border: 1px solid #ddd; padding: 8px; }
        th { background-color: #f4f4f4; }
        .logo { max-width: 150px; } 
        .header { display: flex; align-items: center; margin-bottom: 20px; }
        .header img { margin-right: 20px; }
    </style>
</head>
<body>
    <div class="header">
        <img src="http://localhost:5173/projects/logo" alt="Logo">
        <h1>{{projectData.projectName}}</h1>
    </div>
    <p>Starting Date: {{projectData.startingDate}}</p>
    <p>Customers: {{#each projectData.customerNames}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}</p>
    <p>Employees: {{#each projectData.employeeNames}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}</p>

    <table>
        <thead>
            <tr>
                <th>Date</th>
                <th>Employee</th>
                <th>Hours</th>
                <th>Notes</th>
            </tr>
        </thead>
        <tbody>
            {{generateHoursTable hours}}
        </tbody>
    </table>
    <p>Total Hours: {{totalHours}}</p>
    <p>Price per Hour: ${{projectData.pricePerHours}}</p>
    <p>Total Price: ${{totalPrice}}</p>
</body>
</html>
